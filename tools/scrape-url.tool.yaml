name: scrape-url
description: Scrapes a URL with FireCrawl to capture raw Markdown without rewriting. Supports optional domain-specific prompts.
type: cli
command: "firecrawl scrape {{url}} {{prompt_flag}}"
output_format: markdown
env_vars:
  - FIRECRAWL_API_KEY
